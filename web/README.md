# Frontend

React + Vite + ECharts dashboard. See the [main README](../README.md) for setup.

```bash
pnpm install
pnpm dev     # http://localhost:5173, proxies /api to port 8000
pnpm build   # writes dist/, served by the API
```
